<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinalApplicationActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/brandGray</item>

        <item name="android:windowBackground">@color/colorSurface</item>

        <!-- StatusBar の色も合わせる（透過しない場合）-->
        <item name="android:statusBarColor">@color/brandPink</item>
    </style>

    <style name="Theme.FinalApplicationActivity" parent="Base.Theme.FinalApplicationActivity" />
</resources>